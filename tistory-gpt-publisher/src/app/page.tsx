export default function Home() {
  return (
    <main>
      <div className="card">
        <h1>Tistory GPT Publisher</h1>
        <p>Custom GPT → Vercel → GitHub Actions → Playwright → Tistory 비공개 발행 자동화 서버입니다.</p>
        <p>상태 확인: <code>/api/health</code></p>
      </div>
    </main>
  );
}
